[user]
  name = Edgar Lee
  email = edgar.lee@docker.com
  signingkey = 5437A4D4
[core]
  editor = nvim
  autocrlf = input
  excludesfile = /home/.gitignore
  pager = less
[push]
  default = simple
[commit]
  gpgsign = true
[url "git@github.com:"]
  insteadOf = https://github.com/
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
